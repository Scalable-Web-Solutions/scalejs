<script src="{{ 'test-tag.js' | asset_url }}" defer></script>

<test-tag
         class="scale-{{ section.id }}"
         count="{{ section.settings.count }}"
       ></test-tag>

{% schema %}
{
  "name": "'Hero",
  "settings": [
    {
      "id": "count",
      "label": "count",
      "type": "number",
      "default": 0
    }
  ],
  "blocks": [],
  "presets": [
    {
      "name": "'Hero"
    }
  ]
}
{% endschema %}